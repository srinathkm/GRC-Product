# Node / JS
node_modules/
server/node_modules/
client/node_modules/

# E2E / Playwright (generated)
test-results/
playwright-report/

# Runtime uploads (created by server on contract upload)
server/data/contract-uploads/

# Local JSON/PDF snapshots (restore script / dev only)
server/data/_backups/

# Environment and secrets
.env
.env.*

# Logs and runtime
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# OS / editor
.DS_Store
*.swp
*.swo

# Build artifacts
client/dist/
server/public/
coverage/

# Local archives / images
raqib-docker.tar.gz

# Local runtime stores (governance tests / dev writes; not source of truth)
server/data/runtime/

# Defender data and samples can be regenerated
server/data/defender-uploads.json
server/data/defender-snapshots.json
server/data/defender-scores.json
server/data/defender-findings.json
server/data/defender-summaries.json
server/data/samples/

# M&A saved deal scenarios (may contain MNPI; created at runtime)
server/data/ma-scenarios/scenarios.json
